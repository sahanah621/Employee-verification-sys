import { config } from "./env";

/**
 * IPFS / Pinata configuration placeholder
 */
export const ipfsConfig = {
  jwt: config.ipfs.pinataJwt,
  gateway: config.ipfs.pinataGateway,
  apiUrl: "https://api.pinata.cloud",
};
